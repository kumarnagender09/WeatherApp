package com.mphasis.weather.ui.weather

// Enum class representing the possible states of the search widget (open or closed).
enum class SearchWidgetState {
    // Represents the state when the search widget is open and visible to the user.
    OPENED,

    // Represents the state when the search widget is closed and not visible to the user.
    CLOSED
}
